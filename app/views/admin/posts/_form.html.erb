<%= form_for [:admin, @post] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  
  <% if @post.image.exists? %>
  <p><%= image_tag @post.image %></p>
  <% end %>

  <p>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </p>

  <p><%= f.submit %></p>
<% end %>
